# Base URL of the switchbot-backend API.
# Leave unset (or empty) to use the same origin as the frontend, which is what the
# production container does since the backend serves the built SPA from ./static.
# For local development against a backend on another host/port, set it explicitly:
VITE_API_URL=http://localhost:8000
