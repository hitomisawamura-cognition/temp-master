# Base URL of the Temp Master backend API.
# Leave empty to use the same origin as the frontend (production default).
# During development the Vite dev server proxies /api to http://localhost:8000,
# so this can also stay empty locally.
VITE_API_BASE_URL=
