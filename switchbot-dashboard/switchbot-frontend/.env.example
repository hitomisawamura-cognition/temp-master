# バックエンドの API ベースURL
# - 空文字にすると相対パス（同一オリジン配信、Docker/Fly.io デプロイ時）でアクセスする
# - ローカル開発でバックエンドを別ポートで動かす場合は http://localhost:8000 を指定する
# - 旧 index.html と同じ snakeroom バックエンドを参照する場合は https://snakeroom.fly.dev を指定する
VITE_API_URL=http://localhost:8000
